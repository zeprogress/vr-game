/**
 * Секторы дальности деревьев (VR): центральный конус — полная дальность, боковые
 * полосы — SIDE_K от неё, остальное вокруг не рисуем. Одни и те же числа у VrCull
 * (камни, запасной вариант) и TreeImpostors (модель/снимок).
 */
/** Центральный конус ±37.5° (всего 75°). */
export const COS_CENTER = Math.cos((37.5 * Math.PI) / 180);
/** Боковые полосы по 45° с каждой стороны от конуса (до ±82.5°). */
export const COS_SIDE = Math.cos((82.5 * Math.PI) / 180);
/** Дальность в центре, м. */
export const FAR_CENTER = 250;
/** Дальность в боковых полосах, м (175 / 250). */
export const SIDE_K = 175 / 250;
/** Настоящая 3D-модель дерева — до этого расстояния, дальше снимок. */
export const NEAR_3D = 110;
