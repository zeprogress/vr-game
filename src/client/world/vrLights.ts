/**
 * VR: ночные PointLight'ы декора (костёр лагеря, свет заклинаний) выключены.
 * Каждый включённый источник добавляет член в шейдер ВСЕХ материалов в радиусе
 * и считается на оба глаза — ночью это стоило ~10 fps на Quest.
 */
export const vrLights = {
  /** Костёр лагеря и прочий декор-свет выключены. */
  off: false,
  /** Свет посоха и огнешара ночью оставлен (по просьбе): один-два источника. */
  spell: false,
};
