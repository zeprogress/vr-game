# VR MMORPG — План разработки

> Пет-проект. Соло-разработчик. Цель: VR MMORPG с общим миром, в который можно
> зайти из VR (Quest 2/3, приоритет), из браузера на десктопе (вид от первого лица)
> и с телефона.

---

## 1. Ключевые решения

- **Один клиент на все устройства.** Три источника ввода (VR-контроллеры / клава+мышь /
  тач) сводятся к общему интерфейсу. Геймплейный код не знает, какое устройство.
- **Авторитетный сервер с самого начала (с этапа 4).** Клиент только рисует, сервер
  решает, что произошло. Бой, урон, лут, движение мобов — считает сервер.
- **Одна зона-комната**, не бесшовный мир. «Открытый мир» = позже несколько зон с
  переходами.
- **`shared/` — единственный источник правды** про числа (урон, скорости, радиусы,
  тайминги). Импортируют и клиент, и сервер.

## 2. Стек

| Слой | Технология | Почему |
|---|---|---|
| Язык везде | TypeScript | Один язык клиент+сервер, типобезопасность |
| Движок клиента | Babylon.js 7 | Лучший WebXR из коробки, работает в браузере Quest / десктоп / телефон из одной сборки |
| Сборка клиента | Vite | Быстро, минимум конфигурации |
| Сетевой сервер | Colyseus (Node.js) | Комнаты = зоны, бинарная синхронизация состояния, до десятков игроков. Огромная экономия времени для соло-разработчика |
| БД (сейчас) | SQLite + Prisma | Файл на ПК, ноль настройки |
| БД (потом) | PostgreSQL | Prisma меняет provider одной строкой |
| Кэш/сессии (потом) | Redis | Только когда >1 сервера зон |
| Доступ извне при тесте | Cloudflare Tunnel | Пустить друга на свой ПК-сервер без проброса портов |
| Хостинг потом | VPS + Docker | ~€5–15/мес |

**Почему не Unity/Godot:** WebGL-экспорт тяжёлый и медленно грузится в браузере Quest,
капризен с WebXR. Чистый веб-движок = «везде из одной ссылки» без боли.

## 3. Структура репозитория (целевая, монорепо)

```
vr-game/
  PLAN.md
  shared/                 # общий код клиент<->сервер
    src/
      protocol.ts         # типы сообщений
      constants.ts        # скорость бега, урон, тайминги
      schema/             # Colyseus-схемы состояния (Player, Mob, Loot)
      math/               # хелперы векторов
  client/
    src/
      main.ts
      engine/             # инициализация Babylon, рендер-луп
      input/              # <-- КЛЮЧЕВОЕ: абстракция ввода
        InputSource.ts    # интерфейс: move, look, primaryAction...
        DesktopInput.ts   # клава+мышь
        TouchInput.ts     # экранные стики
        XRInput.ts        # VR-контроллеры
      camera/             # риги камеры под каждый режим
      net/                # клиент Colyseus, интерполяция
      entities/           # визуальное представление игроков/мобов
      world/              # загрузка зоны, коллизии
      ui/                 # чат, HUD, health bar
  server/
    src/
      index.ts
      rooms/ZoneRoom.ts   # одна зона мира
      systems/            # серверная логика (тик 20 Гц)
        MovementSystem.ts
        MobAISystem.ts
        CombatSystem.ts
        LootSystem.ts
      persistence/        # Prisma
      db/schema.prisma
```

> **Сейчас (вариант B)** репозиторий — это просто один Vite-проект в корне
> (`src/`, `index.html`). Реструктуризация в монорепо — на этапе 4, когда добавим
> сервер. Так меньше движущихся частей на старте.

## 4. Архитектура сети

```
Клиент                          Сервер (авторитет, тик 20/сек)
------                          -------------------------------
ввод игрока  ──────────────────▶ применяет ввод к состоянию
                                 симулирует мобов, бой, лут
рисует мир  ◀────────────────── шлёт дельту состояния
интерполирует чужих игроков
предсказывает своё движение (client-side prediction, этап 5+)
```

- Попадания в бою считает сервер. Клиент показывает эффект сразу, урон подтверждает сервер.
- Чужие сущности рисуются с задержкой ~100 мс + интерполяция — выглядит плавно.

## 5. Абстракция ввода

```ts
interface InputState {
  moveVector: Vector2;      // WASD / тачстик / VR-стик
  lookDelta: Vector2;       // мышь / палец / поворот головы
  primaryAction: boolean;   // ЛКМ / кнопка / trigger
  interact: boolean;        // E / кнопка / grip
}
```

- **Desktop:** WASD + мышь (pointer lock), ЛКМ — атака.
- **Touch:** два виртуальных джойстика + кнопки поверх canvas, автоприцел.
- **VR:** левый стик — движение, правый — snap-turn, trigger — атака, grip — взять. Голова = камера.

## 6. Поэтапный план

### Этап 0 — Основы (1–3 недели, обучение)
- Node.js, VS Code, Git.
- Babylon.js Getting Started + «Build a house».
- Пустая сцена на GitHub Pages, открыть с телефона и Quest.

### Этап 1 — Одиночный «ходячий скелет», десктоп ✅ ГОТОВО
- Персонаж-капсула, WASD, mouse-look (pointer lock).
- Коллизии со стенами/землёй, гравитация.
- Простая зона: пол + препятствия.
- **Результат:** бегаешь по зоне от первого лица в браузере.

### Этап 2 — Абстракция ввода + камеры ✅ ГОТОВО
- Вынести ввод в `InputSource`.
- `TouchInput` + экранные стики → проверить на телефоне.

### Этап 3 — VR-режим ✅ ГОТОВО (проверено на Quest)
- `WebXRDefaultExperience`, кнопка «Enter VR».
- Движение стиком + snap-turn (и teleport как опция от укачивания).
- Тест на Quest 2/3.

### Этап 4 — Сервер и мультиплеер-движение ✅ ГОТОВО
> 4a репо, 4b Colyseus, 4c вход по нику, 4d синхр. позиций, 4e интерполяция,
> 4f share.command (Cloudflare Tunnel). Мобы/манекены — client-side до этапа 6.
- Colyseus, одна `ZoneRoom`. Реструктуризация в монорепо.
- Гостевой вход: ник → сервер создаёт `Player`, токен в localStorage.
- Синхронизация позиций, интерполяция чужих.
- Cloudflare Tunnel → тест вдвоём с разных устройств.

### Этап 5 — Сохранение прогресса ✅ ГОТОВО
> JSON-файл (не Prisma) src/server/.data/players.json по гостевому токену.
> Прогресс считает клиент, сервер хранит; серверный сейв главнее онлайн.
- Prisma + SQLite. `Account`(guestToken), `Character`(name, xyz, level, xp, hp).
- Автосейв раз в 30 сек + сейв при выходе.

### Этап 6 — Мобы и серверный AI ✅ ГОТОВО
- `ZoneSim` (без Babylon) на сервере: спавн, idle → chase → attack → dead → respawn
  для слизней и плевунов, полёт плевков. Тик 20 Гц в `ZoneRoom`.
- Общий `src/shared/terrain.ts` — рельеф сервера и клиента совпадает.
- Мобы/куклы/плевки в состоянии; `Mob`/`Dummy` на клиенте стали видом
  (сглаживание, вспышки, раны, звуки). `NetMobs` — менеджер вида.
- Попадание игрока считает клиент → `hm` → сервер применяет. Опыт — `xp`,
  урон по игроку — `mh` (щит/меч гасят на клиенте). Мобов нет в офлайне.
- Осталось на этап 7: серверная проверка геометрии попаданий.

### Этап 7 — Боёвка ✅ ГОТОВО
- Серверная проверка попаданий: цель жива, дистанция в пределах `WEAPON_REACH`,
  темп не выше `WEAPON_RATE`. Урон сервер считает сам из силы игрока.
- HP игрока, реген, смерть с паузой (`RESPAWN.delay`) и возрождение — на сервере.
- Блок щитом/мечом тоже серверный: клиент шлёт положение щита и клинка в
  move-пакете, сервер делает конус-тест (`shared/combat.ts`).
- Опыт, уровни и трата очков характеристик — на сервере (`shared/progression.ts`).
- Клиент рисует эффекты сразу, значения сверяет с состоянием сервера.
- Известное: стрелу по дистанции проверить нельзя (летит через зону) — предел
  задан баллистической дальностью. Полная геометрия клинка отложена.

### Этап 8 — Лут и инвентарь ✅ ГОТОВО
- Моб умер → таблица лута (`shared/items.ts`) → предметы лежат в мире 90 с.
- Подбор автоматический: подошёл ближе `BAG.pickupRadius` — само в сумку.
- Сумка на 8 ячеек со стопками, живёт в состоянии игрока и в сейве.
- Зелье лечения (+40 HP) пьётся из панели; хлам копится на продажу.
- UI — раздел «Сумка» в панели персонажа: на десктопе кнопка «Выпить»,
  в VR X идёт по строкам дальше характеристик по ячейкам, B действует.
- Заодно: 3 с неуязвимости после возрождения (`RESPAWN.invuln`) — раньше
  мобы добивали игрока прямо на точке старта.

### Этап 9 — Голосовой чат ✅ ГОТОВО
> Вместо текстового чата — голос: так решил пользователь.
- Разговор идёт НАПРЯМУЮ между игроками (WebRTC), сервер только сводит их:
  пересылает предложение, ответ и кандидатов (`MSG.rtc`), в содержимое не смотрит.
- Дорожка микрофона открыта, пока микрофон включён (эхо/шум давит браузер);
  замер громкости — только для значка «говорит» и экономии трафика в relay.
- Слышимость: по месту или всех ровно (по умолчанию — ровно) — переключатель
  в панели настройки, там же выключатель микрофона. На десктопе — клавиша M.
- Над говорящим загорается огонёк.
- Свой TURN (coturn) на VPS для NAT/CGNAT.
- Запасной путь, если прямое WebRTC не встало за 7 с: opus через игровой
  сервер (`MSG.voice`, WebCodecs, тот же 443 WSS, что у всех работает).
- Проверено между двумя VR-шлемами — связь есть.

### Этап 10 — PvP ✅ ГОТОВО
> Дружеские дуэли по обоюдному согласию, без штрафа за смерть.
- Флаг `pvp` в `PlayerState`. Урон между игроками проходит, только если у
  ОБОИХ флаг включён.
- `RemoteAvatar` реализует `Hittable`: чужой аватар — цель для меча, стрелы,
  кулака, броска. Клиент шлёт `hm { target: "player" }`, сервер в `tryHit`
  проверяет обоюдный флаг, дистанцию, темп, неуязвимость — и применяет через
  `hurtPlayer` (блок щитом/мечом переиспользуется как есть).
- Урон в PvP × `PVP.damageMult` (0.5) — чтобы дуэль не кончалась с одного
  удара; первая ручка для баланса.
- Снять флаг нельзя, пока с последнего обмена ударами прошло меньше
  `PVP.disengage` (5 с) — нельзя «мигать» флагом в бою.
- Переключение: десктоп — клавиша `P`; VR — строка «PvP» в панели персонажа.
- Над PvP-противником видна полоска здоровья (`hp`/`maxHp` уже в схеме).
- Штрафа за смерть нет, XP за убийство не даётся, голоса не касается.
- Headless-проверено: без флагов урона нет; с флагами проходит; удар по себе
  игнорируется; disengage в бою отклоняется с `wait`, после 5 с — снимается.

### Этап 11 — Прод на VPS ✅ ГОТОВО (иначе, чем планировалось)
> Развёрнуто на `zepgame.duckdns.org`.
- Ubuntu + systemd-сервис (`tsx` напрямую, без Docker) + nginx + HTTPS
  (Let's Encrypt / certbot) + домен + ufw.
- Автодеплой: `git push` → systemd-таймер `vrgame-autopull` подтягивает и
  пересобирает (`deploy/autopull.sh`), ~2 мин.
- coturn (TURN) для голоса (`deploy/setup-turn.sh`).
- Отложено до нагрузки (не блокеры): Docker, Postgres/Prisma (сейчас
  JSON-файлы `players.json` / `world.json`), статика на CDN (раздаёт nginx),
  Redis (нужен только при нескольких серверах зон).

---

### ✅ Этапы 1–11 пройдены (MVP + прод + PvP). Ниже — расширение мира.

> Пять направлений ниже подробно разобраны с вариантами и картинками в
> презентации **«Куда растёт мир»**
> (`claude.ai/code/artifact/128d2982-0d44-489b-8b79-0e7cd2772d5d`).
> Здесь — сжатый план. Всё переиспользует существующие подсистемы по максимуму.

### Этап 12 — Пайплайн внешних ассетов (~1 неделя)
> Вся графика и звук пока процедурные (0 моделей, 0 текстур-картинок). Это
> остаётся основой; внешнее берём точечно — тайлсет подземелья, оружие,
> монстры, весь звук.
- ✅ Загрузчик: `.glb` в `public/models/`, `src/client/world/models.ts` —
  `LoadAssetContainerAsync` + кэш на сцену + `instantiateModelsToScene`,
  glTF-загрузчик отдельным ленивым чанком (~122 КБ gz, не в основном бандле).
- ✅ Перекраска PBR-материалов пака → плоский `StandardMaterial` (эмиссив
  вместо бликов, квантование цвета, 2 света) — против «ассет-флипа».
- ✅ Тест-модель `scripts/gen-test-model.mjs` (ступенчатый постамент,
  бинарный .glb вручную, без зависимостей) + постаменты под стартовым оружием.
- ✅ Природа: Stylized Nature MegaKit (деревья, трава, камни) → `world/nature.ts`.
- ✅ Мобы: слизень (Quaternius) заменил слизня/плевуна/босса; руки — `Hand.glb`.
- Оружие: меч/лук из Ultimate RPG Items Pack (Quaternius, только плоские
  цвета материалов, без текстур). Щита в паке нет — решить отдельно.
- Осталось: сжатие gltfpack/Draco (Babylon 7 понимает без настройки),
  звук Sonniss GDC + Freesound (15–20 базовых) — отложено.
- Убран бесполезный лут (`slime`/`shell`, «на продажу») из предметов и дропа.

### Этап 13 — Крафт оружия (~1 неделя)
- Таблица `RECIPES` в `shared/`; станок-кузница в городе.
- `MSG.craft { recipeId }` → сервер проверяет «рядом с кузницей» + сумку,
  списывает материалы, выдаёт оружие существующим путём («воткнулось в
  мир → берётся рукой»).
- UI — раздел «Кузница» в панели на запястье рядом с «Сумкой».
- Позже: ковка аффиксов (поле `affix` у носимого оружия, синхрон в
  `PlayerState`, эффект в расчёте попадания — делит статусы с магией).

### Этап 14 — Посох и магия (~2–3 недели, срезами)
> «Жёсткая» магия: `mana`/`maxMana`, а также сила / радиусы / длительность
> эффектов — от `int`. Кулдауны, ману, LoS, дистанции — всё проверяет сервер.
> Свитков нет: вся магия идёт через посох.

**Посох (модель + хват).**
- Модель: тонкое древко + кристалл из пака (Quaternius) на верхнем конце
  (светится эмиссивом).
- Древко делится на 3 равные части; два стыка — точки хвата (верхняя и нижняя).
  Можно взять одной рукой за любую точку или двумя руками сразу (по точке на руку).
- Физический удар как мечом (детект по скорости кончика), но урон заметно
  меньше базового меча — посох это прежде всего фокус для магии.
- Класс оружия `staff`; лежит в мире и берётся рукой существующим путём.

**Каст.**
- Триггер на держащей руке — каст выбранного заклинания из кончика посоха
  (направление = ось посоха). Мах посохом при этом = обычный физический удар.
- Выбор заклинания — колесо на стике / второй руке.
- `MSG.cast { spellId, origin, dir }` → сервер валидирует ману/кд/LoS/дистанцию.

**Школы заклинаний (боевые и небоевые).**
- Урон: снаряд (путь плевка), AoE-чары на земле (кольцо слэма босса).
- Лечение: себя и союзника (heal зелья).
- Телекинез: тянуть/толкать лут и лёгкие пропы (новый серверный хват объекта).
- Дебаффы на мобах: `burnUntil`, `slowUntil`, … (статусы на `MobState`).
- Баффы союзников: временный множитель урона / скорости / брони (`PlayerState`).
- Кооп-синергия: два одинаковых каста рядом → усиленный эффект (серверно).
- VFX — GPU-частицы (1 draw-call), свечение эмиссивом, без `PointLight`.
- Жесты-глифы (`$1`-распознаватель) — отложены, отдельный R&D.

### Этап 14б — Баланс оружия и разнообразие врагов
> Проверка 2026-09: посох (огнешар) имбовый — дистанционный ваншот с фэт-хитбоксом,
> нулевой риск. Лук — самое слабое: `1 × mult`, ловкость даёт только скорость полёта.
> Ловкость как стат почти мёртвая. Каждый бой = 5 одинаковых ближних мобов в поле,
> поэтому дистанционный AoE побеждает всегда — это первопричина.

**Зафиксированные решения:**
- Роль статов: сила → ближний урон (+HP), ловкость → дальний урон + скорость
  действий (+бег), интеллект → сила/экономика магии (+мана).
- D1: у огнешара будет **сплэш** (попадание бьёт всех в радиусе на ~35%) — посох
  становится ответом на рой, одиночный урон падает сильнее. *(отложено — новая система)*
- D3/D4: враги и «скорость атаки от ловкости» — отдельным заходом, не сейчас.

**Сделано сразу (только числа):**
- Посох вниз: `firebolt.baseDamage 1.5→1.0`, `damagePerCharge 5.5→2.8`,
  `intScale 0.06→0.03`, `cooldown 0.5→0.9`, кривая заряда разглажена; хитбокс
  снаряда отделён от визуала (`fireboltHitRadius` ≈ 0.12 + заряд·0.18, было 0.62).
- Лук вверх: урон стрелы масштабируется от ловкости (`arrowDamageFor`), база `1→1.3`.

**Отложено (нужен пламбинг сообщений / новые системы):**
- Урон стрелы от натяга + хедшот ×1.8 — клиент шлёт `power`/`head` в `HitMobMsg`.
- Скорость атаки от ловкости — множитель на кулдаун маха / нока / заряда.
- Враги: броневик (8 HP, −50% урона, медленный, телеграф) и рой (2 HP, пачка 5,
  быстрый). Ломают «огнешар всё»: броневик не берётся чипом, рой — размашистый
  мах меча (VR-мах уже бьёт всех на дуге) или сплэш огнешара.
- Огнешар-сплэш (D1).
- Небоевая магия (хил / телекинез / баффы) — идентичность посоха, см. Этап 14.

### Этап 15 — Подземелье (~1 месяц) — самый крупный трек
> Даёт материалы для крафта и повод в него ходить.
- Новый тип комнаты `DungeonRoom`, инстанс на группу 2–4, вход через портал на лугу.
- Генерация за интерфейсом `generate(seed)`; первое подземелье —
  курируемые префаб-комнаты + процедурный граф коридоров (стиль Isaac);
  BSP/пещеры подключаются позже как другие реализации.
- Тайлсет — Kenney Modular Dungeon Kit (CC0). Коллизии — список
  прямоугольников-стен (приём стволов деревьев), не raycast.
- Новые мобы на базе класса `Mob` + один флаг: скелет-лучник (логика
  плевуна), рой мышей (осколки), голем (слэм босса), босс (каркас `BOSS`).
  Гуманоидам нужна модель + анимации (Quaternius).
- Ловушки: триггер → механизм → выброс, всё телеграфится. 2 типа
  (дротики + глиф-AoE на торусе слэма). Состояние и урон — на сервере.
- Одна запертая дверь + ключ (обычный предмет в сумке, проверка на сервере).
- Телепорт-перемещение здесь по умолчанию.
- Лут: руда/кость/кристалл, повышенный шанс золотого оружия, багровое ядро с босса.

### Этап 16 — Лагерь и сбор ресурсов (~2–3 недели)
- Групповой участок через «портал лагеря»; персистентность — расширение `WorldStore`.
- Сбор: топор/кирка по узлам с «HP» (детект удара как по мобу, респавн по
  таймеру); подбор руками (путь лута); разбор хлама (слизь/панцирь → волокно).
- Постройка — функциональные пропы: костёр (готовка/реген), сундук (общее
  хранилище группы), знамя (точка возрождения группы), кузница (этап 13),
  вардстоун (мобы вне радиуса), факелы (свет эмиссивом).
- Ввод — «строительный молот»: призрак пропа, курок ставит, grip убирает.
  Прицел — тот же код дуги, что у телепорта.
- Позже: снап-сетка стен (структурная постройка).

**Порядок (обновлён пользователем, 2026-09):**
модели оружия (меч/лук/щит из пака Quaternius) → **14 — посох и магия** →
**15 — подземелье** (без босса) → **13 — крафт** → **16 — лагерь и стройка** →
15-босс. Сначала доводим оружие и магию, потом подземелье как площадка, потом
крафт и стройка, которые ими питаются.

---

### Этап 17 — Стрим на Twitch (ZEP GAME) · параллельный трек

> Отдельная ветка, не мешает этапам 12–16. Цель: невидимый спектатор внутри
> игры с набором камер + пульт с телефона + вывод на Twitch.

**Железо.** VPS (1 ядро / 1 ГБ) рендер не тянет. Рендерит **Android-бокс
TOX3** (Amlogic S905X4, Mali-G31, 4 ГБ, Ethernet, HW-энкодер, 24/7): в нём
Chromium (Fully Kiosk) открывает страницу спектатора, Larix Screencaster
снимает экран + звук страницы → RTMP на Twitch. ПК свободен (VR идёт на
Quest). Целевое качество 1080p; у спектатора пресеты, замер на боксе решит
60/30 fps. Фолбэк — та же страница в Chrome на ПК + OBS.

**Архитектура.**
```
Quest (игра) ─► Colyseus ZoneRoom (VPS)
                     │  + вход спектатора по SPECTATOR_KEY (нет PlayerState,
                     │    не игрок, не агрит мобов, переживает деплой)
                     │  + реле команд: дашборд ─► спектатор
   TOX3 ◄────────────┘
   Fully Kiosk → /?spectator=KEY   (рендер сцены, камеры, оверлеи в странице)
   Larix → HW H.264 → RTMP → Twitch  (экран + Web Audio страницы)
        ▲ команды камер
   Дашборд (веб с VPS, /dash, токен) — телефон/ПК
```

**Звук стрима** — SFX и музыка игры со страницы спектатора; отдельного
микрофона нет. Позже спектатор слушает войс-чат игроков (это и есть
«комментарий»; игроки должны знать, что в эфире).

**Фазы (порядок — как просил пользователь: сначала на стрим, потом красота):**

| Ф | Что | Где тест |
|---|---|---|
| **Ф1** ✅ | Спектатор-режим (`?spectator=КЛЮЧ`): невидим, не игрок, переживает деплой. Автономный режиссёр (обзор ↔ орбита игрока ↔ орбита босса), смена ~20 с с блендом. Пресеты `?q=potato\|low\|med\|high` (рендер-скейл, трава, светлячки, облака, 2 света, облегчённые мобы). `?rw=&rh=` фикс. рендера, `?debug=1`. Звук + музыка игры. | ПК |
| **Ф2** | Настройка TOX3: HDMI-заглушка, Fully Kiosk, Larix. Бокс тянет `q=low` ~26–30 fps на 720–1080p (Mali-G31 слабый). Звук игры — через HDMI-аудиоэкстрактор (внутренний захват на Amlogic не работает). **Первый эфир ZEP GAME, hands-off.** | TOX3 |
| **Ф3** ✅ | Камеры из глаз игроков (сглаж., `?rawcam=1` — сырой) и мобов (слизень/плевун/босс, авто-переключение при смерти/деспавне). Во время боя с боссом — своя ротация: орбита босса ↔ из глаз босса ↔ из глаз ближнего игрока. Бленды между всеми. | ПК |
| **Ф4** ✅ | Кинематографичные пути (`src/client/spectator/cine.ts` — 3 сплайновых пролёта Катмулла–Рома по координатам мира) + настраиваемый порядок ротации (`ROTATION` — токены `overview·orbitPlayer·eyePlayer·orbitBoss·eyeMob·path:N`, невалидные пропускаются). Путь идёт до конца своей длительности, остальные кадры — `holdTime`. | ПК |
| **Ф5** ✅ | Реле команд в Colyseus (`MSG.specCmd`, только для авторизованного спектатора) + дашборд (`/?dash=КЛЮЧ`, ключ в localStorage): авто-режиссёр вкл/выкл, фикс-кадры (обзор/орбита босса/из глаз босса/cut), кинопути, живой список игроков+мобов с кнопками орбита/из-глаз, время суток. Спектатор шлёт `nowShot` обратно на пульт. | ПК + телефон |
| **Ф6** ✅ | `Overlay.ts` поверх canvas (полное разрешение вьюпорта): вотермарк «ZEP GAME», часы мира + таймер сессии, список онлайн, плашка «смотрим: ник/моб», HP-бар цели (красный у босса), нижняя треть/заставка. `SpecCmd card {title,sub,secs}` с пульта («показать 6 с» / «держать 60 с»). `?overlay=0` — выкл. | ПК |
| **Ф7** | Спектатор в войс-чате (звук эфира), heartbeat страницы → VPS → Telegram-алерт, «экран ожидания» + авто-стоп эфира через 10 мин пустого сервера. | TOX3 |
| **Ф8** | Превью эфира в дашборде (WebRTC), удалённый старт/стоп (Tasker/webhook на боксе), локальная запись VOD. | — |
| **Ф7** | ⏸ отложено (нужна настройка бокса/VPS): войс в эфире, heartbeat→Telegram, экран ожидания, авто-стоп. |
| **Ф8** | ⏸ отложено (до бокса не дотянуться): WebRTC-превью, удалённый старт/стоп, VOD. |
| **Ф9** (опц.) | Авто-режиссёр ✅ (Ф1/Ф3). Кил-фид ✅ — лента справа, тумблер на пульте. Осталось: чат Twitch + команды (`!cam boss`), редактор путей в игре, Twitch API (название/категория). | — |

**Секреты.** `SPECTATOR_KEY` — встроен в `shared/constants.ts` (репо
приватный, спектатор всё равно только смотрит). Смена ключа = правка +
`git push`, ноль команд на VPS. `deploy/stream.env` + `EnvironmentFile` в
systemd — задел для дашборда/алертов, применяется позже и по желанию.
Ключ Twitch — только в Larix на TOX3.

## 7. Подводные камни

1. **WebXR требует HTTPS.** Локально `localhost` ок, по сети — только туннель/сертификат.
2. **Браузер Quest слаб.** <100k полигонов в кадре, <50 мешей, простые шейдеры.
3. **Укачивание в VR.** Дать teleport + snap-turn на выбор.
4. **Тач в 3D-экшене неудобен.** Автоприцел / область атаки, не точное наведение.
5. **Не делать бесшовный мир сразу.** Одна зона-комната.
6. **Авторитет сервера с начала.** Переделка client→server authoritative = переписать половину.
7. **`shared/` дисциплина.** Любое общее число — только там.

## 8. Оценка сроков

MVP (этапы 1–9) при работе по вечерам в одиночку: **6–12 месяцев**. Это нормально.

Статус: **этапы 1–11 пройдены** (MVP + прод + PvP). Дальше — расширение
мира (этапы 12–16), ещё ~2–3 месяца вечерами, срезами.

## MVP (определение готовности)

Одна зона, 1–5 игроков в общем мире, вход из VR и из браузера, беготня + чат +
простая боёвка по мобам + подбор лута + сохранение прогресса. — **достигнут.**

Сверх MVP уже в игре (в план не занесено этапами): босс с фазами (деление,
рывок-таран, плевок, ярость, своя музыка), цикл суток + перевод времени
админом, телепорт-перемещение и комфорт-виньетка с глобальными тумблерами,
админ-панель (время, комфорт, очистка мира от лута), лут на земле с
персистентностью мира (`WorldStore`), сохранение экипировки, железный/золотой
тиры оружия, LAN-шеринг, запрос макс. частоты кадров у шлема.
